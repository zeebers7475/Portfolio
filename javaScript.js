const button = document.getElementsByTagName('button');

const showPic = async (event) => {
    let list = document.getElementById('projects').getElementsByTagName('li');
    const pic = document.getElementsByClassName('project-img');
    let nextPic = 
    console.log(event.target)
   

    //pic[0].style.display = 'block';
};

button[0].addEventListener('click', showPic);
button[1].addEventListener('click', showPic);
button[2].addEventListener('click', showPic);